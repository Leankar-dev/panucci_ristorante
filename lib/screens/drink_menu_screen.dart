import 'package:flutter/material.dart';

class DrinkMenuScreen extends StatelessWidget {
  const DrinkMenuScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('drink')),
      body: Container(),
    );
  }
}
